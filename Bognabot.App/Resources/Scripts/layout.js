import vue from 'vue';

export class LayoutViewModel {
    constructor(options) {
        this.vm = new vue({
            el: '#main',
            data: {
                
            }
        });
    }
}